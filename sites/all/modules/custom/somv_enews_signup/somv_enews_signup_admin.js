(function($) {
  $(document).ready(function () {
    var $form = $('.node-resident_address-form');
    $form.find('.form-item-title').hide();
    $('#edit-title').val('placeholder');
  });
})(jQuery);
